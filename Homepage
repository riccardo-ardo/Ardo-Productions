<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Ardo Productions | Design. Build. Activate.</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">
<style>
/* ============================================================
   RESET & BASE
============================================================ */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg: #06060a;
  --bg2: #0d0d14;
  --bg3: #12121c;
  --surface: rgba(255,255,255,0.04);
  --surface-hover: rgba(255,255,255,0.07);
  --border: rgba(255,255,255,0.08);
  --border-bright: rgba(255,255,255,0.15);
  --text: #f0ede8;
  --text-muted: rgba(240,237,232,0.55);
  --text-dim: rgba(240,237,232,0.35);
  --accent-purple: #a78bfa;
  --accent-cyan: #22d3ee;
  --accent-pink: #f472b6;
  --accent-amber: #fbbf24;
  --grad-1: linear-gradient(135deg, #a78bfa, #22d3ee);
  --grad-2: linear-gradient(135deg, #f472b6, #a78bfa);
  --grad-3: linear-gradient(135deg, #22d3ee, #a78bfa, #f472b6);
  --font-display: 'Syne', sans-serif;
  --font-body: 'DM Sans', sans-serif;
  --radius: 20px;
  --radius-lg: 28px;
  --ease: cubic-bezier(0.16, 1, 0.3, 1);
}

html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: var(--font-body);
  font-size: 16px;
  line-height: 1.6;
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
}

/* ============================================================
   CUSTOM CURSOR
============================================================ */
.cursor {
  position: fixed;
  width: 10px; height: 10px;
  background: var(--accent-cyan);
  border-radius: 50%;
  pointer-events: none;
  z-index: 9999;
  transform: translate(-50%, -50%);
  transition: width 0.3s var(--ease), height 0.3s var(--ease), background 0.3s;
  mix-blend-mode: screen;
}
.cursor-ring {
  position: fixed;
  width: 36px; height: 36px;
  border: 1px solid rgba(167, 139, 250, 0.5);
  border-radius: 50%;
  pointer-events: none;
  z-index: 9998;
  transform: translate(-50%, -50%);
  transition: all 0.15s var(--ease);
}
body:has(a:hover) .cursor, body:has(button:hover) .cursor {
  width: 20px; height: 20px;
  background: var(--accent-purple);
}

/* ============================================================
   NOISE GRAIN OVERLAY
============================================================ */
body::before {
  content: '';
  position: fixed;
  inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
  pointer-events: none;
  z-index: 9000;
  opacity: 0.4;
}

/* ============================================================
   SCROLL REVEAL
============================================================ */
.reveal {
  opacity: 0;
  transform: translateY(32px);
  transition: opacity 0.8s var(--ease), transform 0.8s var(--ease);
}
.reveal.visible {
  opacity: 1;
  transform: translateY(0);
}
.reveal-delay-1 { transition-delay: 0.1s; }
.reveal-delay-2 { transition-delay: 0.2s; }
.reveal-delay-3 { transition-delay: 0.3s; }
.reveal-delay-4 { transition-delay: 0.4s; }
.reveal-delay-5 { transition-delay: 0.5s; }

/* ============================================================
   NAV
============================================================ */
nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  padding: 20px 48px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: background 0.4s, backdrop-filter 0.4s, border-bottom 0.4s;
}
nav.scrolled {
  background: rgba(6,6,10,0.85);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border-bottom: 0.5px solid var(--border);
}
.nav-logo {
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 800;
  letter-spacing: -0.03em;
  background: var(--grad-1);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-decoration: none;
}
.nav-links {
  display: flex;
  align-items: center;
  gap: 36px;
  list-style: none;
}
.nav-links a {
  font-family: var(--font-body);
  font-size: 14px;
  font-weight: 400;
  color: var(--text-muted);
  text-decoration: none;
  letter-spacing: 0.01em;
  transition: color 0.25s;
  position: relative;
}
.nav-links a::after {
  content: '';
  position: absolute;
  bottom: -4px; left: 0; right: 0;
  height: 1px;
  background: var(--grad-1);
  transform: scaleX(0);
  transition: transform 0.3s var(--ease);
}
.nav-links a:hover { color: var(--text); }
.nav-links a:hover::after { transform: scaleX(1); }
.nav-cta {
  padding: 10px 22px;
  border-radius: 999px;
  background: var(--surface);
  border: 0.5px solid var(--border-bright);
  color: var(--text) !important;
  font-weight: 500 !important;
  font-size: 13px !important;
  backdrop-filter: blur(12px);
  transition: background 0.25s, border-color 0.25s !important;
}
.nav-cta::after { display: none !important; }
.nav-cta:hover {
  background: var(--surface-hover) !important;
  border-color: rgba(255,255,255,0.25) !important;
}

/* availability dot */
.avail-badge {
  display: flex;
  align-items: center;
  gap: 7px;
  padding: 7px 14px;
  border-radius: 999px;
  background: rgba(34, 197, 94, 0.08);
  border: 0.5px solid rgba(34, 197, 94, 0.2);
  font-size: 12px;
  color: rgba(134, 239, 172, 0.9);
  font-weight: 500;
  letter-spacing: 0.02em;
}
.avail-dot {
  width: 6px; height: 6px;
  border-radius: 50%;
  background: #4ade80;
  box-shadow: 0 0 8px #4ade80;
  animation: pulse-green 2s ease-in-out infinite;
}
@keyframes pulse-green {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.6; transform: scale(0.8); }
}

/* ============================================================
   HERO
============================================================ */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding: 120px 48px 80px;
  overflow: hidden;
}

/* animated gradient orbs */
.orb {
  position: absolute;
  border-radius: 50%;
  filter: blur(80px);
  pointer-events: none;
  animation: float-orb 8s ease-in-out infinite;
}
.orb-1 {
  width: 600px; height: 600px;
  background: radial-gradient(circle, rgba(167,139,250,0.18) 0%, transparent 70%);
  top: -200px; left: -100px;
  animation-delay: 0s;
}
.orb-2 {
  width: 500px; height: 500px;
  background: radial-gradient(circle, rgba(34,211,238,0.14) 0%, transparent 70%);
  top: -100px; right: -50px;
  animation-delay: -3s;
}
.orb-3 {
  width: 400px; height: 400px;
  background: radial-gradient(circle, rgba(244,114,182,0.10) 0%, transparent 70%);
  bottom: 0; left: 30%;
  animation-delay: -6s;
}
@keyframes float-orb {
  0%, 100% { transform: translateY(0) translateX(0) scale(1); }
  33% { transform: translateY(-30px) translateX(20px) scale(1.05); }
  66% { transform: translateY(20px) translateX(-15px) scale(0.95); }
}

/* grid */
.hero-grid {
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
  background-size: 64px 64px;
  mask-image: radial-gradient(ellipse 80% 60% at 50% 40%, black 30%, transparent 80%);
}

.hero-inner {
  position: relative;
  z-index: 2;
  max-width: 1380px;
  margin: 0 auto;
  width: 100%;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 48px;
  align-items: center;
}

.hero-copy { max-width: 700px; }

.hero-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 8px 16px;
  border-radius: 999px;
  background: rgba(167,139,250,0.08);
  border: 0.5px solid rgba(167,139,250,0.25);
  font-size: 12px;
  font-weight: 500;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--accent-purple);
  margin-bottom: 28px;
  animation: fade-up 0.8s var(--ease) 0.1s both;
}
.hero-eyebrow-line {
  width: 24px; height: 1px;
  background: var(--accent-purple);
  opacity: 0.6;
}

.hero h1 {
  font-family: var(--font-display);
  font-size: clamp(3.4rem, 5.5vw, 6.4rem);
  font-weight: 800;
  line-height: 0.93;
  letter-spacing: -0.05em;
  color: var(--text);
  margin-bottom: 28px;
  animation: fade-up 0.9s var(--ease) 0.2s both;
}
.hero h1 .grad-text {
  background: var(--grad-3);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  background-size: 200%;
  animation: gradient-shift 4s ease infinite;
}
@keyframes gradient-shift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.hero-sub {
  font-size: 18px;
  line-height: 1.75;
  color: var(--text-muted);
  max-width: 560px;
  margin-bottom: 40px;
  font-weight: 300;
  animation: fade-up 1s var(--ease) 0.3s both;
}

.hero-ctas {
  display: flex;
  gap: 14px;
  flex-wrap: wrap;
  margin-bottom: 56px;
  animation: fade-up 1s var(--ease) 0.4s both;
}

.btn-primary {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 16px 30px;
  border-radius: 999px;
  background: linear-gradient(135deg, #a78bfa, #22d3ee);
  color: #06060a;
  font-family: var(--font-body);
  font-size: 14px;
  font-weight: 700;
  letter-spacing: 0.02em;
  text-decoration: none;
  border: none;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  transition: transform 0.3s var(--ease), box-shadow 0.3s;
  box-shadow: 0 0 30px rgba(167,139,250,0.3);
}
.btn-primary::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, #c4b5fd, #67e8f9);
  opacity: 0;
  transition: opacity 0.3s;
}
.btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 40px rgba(167,139,250,0.4); }
.btn-primary:hover::before { opacity: 1; }
.btn-primary span { position: relative; z-index: 1; }

.btn-secondary {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 16px 30px;
  border-radius: 999px;
  background: var(--surface);
  border: 0.5px solid var(--border-bright);
  color: var(--text);
  font-family: var(--font-body);
  font-size: 14px;
  font-weight: 500;
  text-decoration: none;
  backdrop-filter: blur(16px);
  transition: background 0.3s, border-color 0.3s, transform 0.3s var(--ease);
}
.btn-secondary:hover {
  background: var(--surface-hover);
  border-color: rgba(255,255,255,0.2);
  transform: translateY(-2px);
}

.hero-stats {
  display: flex;
  gap: 40px;
  animation: fade-up 1s var(--ease) 0.5s both;
}
.hero-stat {}
.hero-stat-num {
  font-family: var(--font-display);
  font-size: 28px;
  font-weight: 800;
  letter-spacing: -0.04em;
  background: var(--grad-1);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1;
  margin-bottom: 4px;
}
.hero-stat-label {
  font-size: 12px;
  color: var(--text-dim);
  letter-spacing: 0.05em;
  text-transform: uppercase;
}

/* hero right panel */
.hero-panel {
  display: flex;
  flex-direction: column;
  gap: 16px;
  animation: fade-up 1.1s var(--ease) 0.3s both;
}

.glass-card {
  background: rgba(255,255,255,0.04);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 0.5px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 28px;
  position: relative;
  overflow: hidden;
  transition: border-color 0.3s, background 0.3s, transform 0.3s var(--ease);
}
.glass-card::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(255,255,255,0.04) 0%, transparent 60%);
  pointer-events: none;
}
.glass-card:hover {
  border-color: var(--border-bright);
  background: rgba(255,255,255,0.06);
  transform: translateY(-2px);
}

.glass-card-main {
  min-height: 240px;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
}
.glass-card-main::after {
  content: '';
  position: absolute;
  top: -60px; right: -60px;
  width: 220px; height: 220px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(167,139,250,0.2) 0%, transparent 70%);
  filter: blur(30px);
}

.card-tag {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 5px 12px;
  border-radius: 999px;
  background: rgba(167,139,250,0.12);
  border: 0.5px solid rgba(167,139,250,0.25);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--accent-purple);
  margin-bottom: 16px;
}

.glass-card-main h3 {
  font-family: var(--font-display);
  font-size: 26px;
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.03em;
  color: var(--text);
  margin-bottom: 12px;
}
.glass-card-main p {
  font-size: 14px;
  line-height: 1.7;
  color: var(--text-muted);
}

.hero-panel-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
}
.hero-panel-row .glass-card {
  padding: 22px;
}
.mini-label {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--text-dim);
  margin-bottom: 12px;
}
.mini-items {
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.mini-item {
  font-size: 13px;
  color: var(--text-muted);
  display: flex;
  align-items: center;
  gap: 8px;
}
.mini-item::before {
  content: '';
  width: 4px; height: 4px;
  border-radius: 50%;
  background: var(--accent-cyan);
  flex-shrink: 0;
}

@keyframes fade-up {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ============================================================
   SERVICES
============================================================ */
.section {
  padding: 120px 48px;
  max-width: 1380px;
  margin: 0 auto;
}

.section-full {
  padding: 120px 48px;
}
.section-full .section-inner {
  max-width: 1380px;
  margin: 0 auto;
}

.section-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--text-dim);
  margin-bottom: 20px;
}
.section-eyebrow::before {
  content: '';
  width: 24px; height: 1px;
  background: var(--grad-1);
}

.section-heading {
  font-family: var(--font-display);
  font-size: clamp(2.2rem, 4vw, 4rem);
  font-weight: 800;
  letter-spacing: -0.04em;
  line-height: 0.95;
  color: var(--text);
  margin-bottom: 16px;
}

.section-sub {
  font-size: 17px;
  line-height: 1.75;
  color: var(--text-muted);
  max-width: 560px;
  font-weight: 300;
}

.services-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  margin-bottom: 56px;
  gap: 32px;
}

.services-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

/* Wix dev tiles span differently */
.service-tile {
  background: var(--surface);
  border: 0.5px solid var(--border);
  border-radius: var(--radius);
  padding: 32px;
  position: relative;
  overflow: hidden;
  transition: border-color 0.4s, background 0.4s, transform 0.4s var(--ease);
  cursor: default;
}
.service-tile::after {
  content: '';
  position: absolute;
  inset: 0;
  opacity: 0;
  transition: opacity 0.4s;
  border-radius: var(--radius);
}
.service-tile:hover {
  border-color: var(--border-bright);
  background: rgba(255,255,255,0.06);
  transform: translateY(-4px);
}
.service-tile:hover::after { opacity: 1; }

/* color accents per tile */
.tile-purple::after { background: radial-gradient(circle at top left, rgba(167,139,250,0.08) 0%, transparent 60%); }
.tile-cyan::after   { background: radial-gradient(circle at top left, rgba(34,211,238,0.07) 0%, transparent 60%); }
.tile-pink::after   { background: radial-gradient(circle at top left, rgba(244,114,182,0.07) 0%, transparent 60%); }
.tile-amber::after  { background: radial-gradient(circle at top left, rgba(251,191,36,0.07) 0%, transparent 60%); }
.tile-teal::after   { background: radial-gradient(circle at top left, rgba(45,212,191,0.07) 0%, transparent 60%); }

.service-icon {
  width: 48px; height: 48px;
  border-radius: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 24px;
  font-size: 22px;
  position: relative;
  z-index: 1;
}
.icon-purple { background: rgba(167,139,250,0.12); border: 0.5px solid rgba(167,139,250,0.2); }
.icon-cyan   { background: rgba(34,211,238,0.10);  border: 0.5px solid rgba(34,211,238,0.2);  }
.icon-pink   { background: rgba(244,114,182,0.10); border: 0.5px solid rgba(244,114,182,0.2); }
.icon-amber  { background: rgba(251,191,36,0.10);  border: 0.5px solid rgba(251,191,36,0.2);  }
.icon-teal   { background: rgba(45,212,191,0.10);  border: 0.5px solid rgba(45,212,191,0.2);  }

.service-name {
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 700;
  letter-spacing: -0.02em;
  color: var(--text);
  margin-bottom: 10px;
  position: relative;
  z-index: 1;
}
.service-desc {
  font-size: 14px;
  line-height: 1.7;
  color: var(--text-muted);
  margin-bottom: 20px;
  position: relative;
  z-index: 1;
}
.service-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  position: relative;
  z-index: 1;
}
.service-tag {
  padding: 4px 10px;
  border-radius: 999px;
  font-size: 11px;
  font-weight: 500;
  background: rgba(255,255,255,0.05);
  border: 0.5px solid rgba(255,255,255,0.08);
  color: var(--text-dim);
}

/* Wix tiles — featured */
.tile-wix {
  background: linear-gradient(135deg, rgba(167,139,250,0.07), rgba(34,211,238,0.05));
  border-color: rgba(167,139,250,0.2);
}
.tile-wix:hover {
  border-color: rgba(167,139,250,0.35);
}
.wix-badge {
  position: absolute;
  top: 20px; right: 20px;
  padding: 4px 10px;
  border-radius: 999px;
  background: var(--grad-1);
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #06060a;
}

/* ============================================================
   TRUST STRIP
============================================================ */
.trust-section {
  padding: 0 0 80px;
  overflow: hidden;
}
.trust-label {
  text-align: center;
  font-size: 12px;
  font-weight: 600;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--text-dim);
  margin-bottom: 36px;
}
.trust-track-wrap {
  position: relative;
  overflow: hidden;
}
.trust-track-wrap::before,
.trust-track-wrap::after {
  content: '';
  position: absolute;
  top: 0; bottom: 0;
  width: 120px;
  z-index: 2;
  pointer-events: none;
}
.trust-track-wrap::before { left: 0; background: linear-gradient(90deg, var(--bg), transparent); }
.trust-track-wrap::after  { right: 0; background: linear-gradient(-90deg, var(--bg), transparent); }

.trust-track {
  display: flex;
  gap: 56px;
  animation: scroll-logos 25s linear infinite;
  width: max-content;
}
.trust-track:hover { animation-play-state: paused; }

@keyframes scroll-logos {
  0%   { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}

.logo-pill {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 14px 28px;
  border-radius: 999px;
  background: var(--surface);
  border: 0.5px solid var(--border);
  color: var(--text-dim);
  font-family: var(--font-display);
  font-size: 14px;
  font-weight: 700;
  letter-spacing: 0.04em;
  white-space: nowrap;
  transition: color 0.3s, border-color 0.3s;
}
.logo-pill:hover {
  color: var(--text-muted);
  border-color: var(--border-bright);
}

/* ============================================================
   PORTFOLIO PLACEHOLDER
============================================================ */
.portfolio-placeholder {
  margin: 0 48px 80px;
  border-radius: var(--radius-lg);
  background: var(--surface);
  border: 0.5px solid var(--border);
  border: 1px dashed rgba(255,255,255,0.12);
  padding: 80px 40px;
  text-align: center;
  position: relative;
  overflow: hidden;
}
.portfolio-placeholder::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(ellipse 60% 40% at 50% 50%, rgba(167,139,250,0.05) 0%, transparent 70%);
}
.portfolio-placeholder-inner { position: relative; z-index: 1; }
.portfolio-placeholder-icon {
  font-size: 40px;
  margin-bottom: 16px;
  opacity: 0.5;
}
.portfolio-placeholder h3 {
  font-family: var(--font-display);
  font-size: 22px;
  font-weight: 700;
  color: var(--text-muted);
  margin-bottom: 8px;
}
.portfolio-placeholder p {
  font-size: 14px;
  color: var(--text-dim);
}

/* ============================================================
   ABOUT
============================================================ */
.about-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 80px;
  align-items: center;
}

.about-visual {
  position: relative;
}
.about-image-frame {
  width: 100%;
  aspect-ratio: 4/5;
  border-radius: var(--radius-lg);
  background: linear-gradient(135deg, rgba(167,139,250,0.1), rgba(34,211,238,0.05));
  border: 0.5px solid var(--border);
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: flex-end;
}
.about-image-frame::before {
  content: '';
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 30% 30%, rgba(167,139,250,0.15), transparent 50%),
    radial-gradient(circle at 70% 70%, rgba(34,211,238,0.1), transparent 50%);
}
.about-image-placeholder {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 80px;
  opacity: 0.1;
}
.about-name-card {
  position: relative;
  z-index: 2;
  width: 100%;
  padding: 24px;
  background: rgba(6,6,10,0.7);
  backdrop-filter: blur(20px);
  border-top: 0.5px solid var(--border);
}
.about-name {
  font-family: var(--font-display);
  font-size: 18px;
  font-weight: 700;
  color: var(--text);
  margin-bottom: 4px;
}
.about-title-tag {
  font-size: 13px;
  color: var(--text-muted);
}

.floating-badge {
  position: absolute;
  top: 32px; right: -20px;
  padding: 12px 18px;
  border-radius: var(--radius);
  background: rgba(6,6,10,0.9);
  backdrop-filter: blur(16px);
  border: 0.5px solid var(--border-bright);
  box-shadow: 0 20px 60px rgba(0,0,0,0.4);
}
.floating-badge-num {
  font-family: var(--font-display);
  font-size: 24px;
  font-weight: 800;
  background: var(--grad-2);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1;
}
.floating-badge-text {
  font-size: 11px;
  color: var(--text-dim);
  margin-top: 2px;
}

.about-copy {}
.about-copy .section-heading { margin-bottom: 24px; }
.about-body {
  font-size: 16px;
  line-height: 1.85;
  color: var(--text-muted);
  font-weight: 300;
  margin-bottom: 16px;
}
.about-body strong { color: var(--text); font-weight: 500; }

.about-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
  margin-top: 40px;
}
.about-stat {
  padding: 20px;
  border-radius: var(--radius);
  background: var(--surface);
  border: 0.5px solid var(--border);
  text-align: center;
  transition: border-color 0.3s, background 0.3s;
}
.about-stat:hover { border-color: var(--border-bright); background: var(--surface-hover); }
.about-stat-num {
  font-family: var(--font-display);
  font-size: 32px;
  font-weight: 800;
  letter-spacing: -0.04em;
  background: var(--grad-1);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1;
  margin-bottom: 6px;
}
.about-stat-label {
  font-size: 12px;
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

/* ============================================================
   PROCESS
============================================================ */
.process-section {
  padding: 120px 48px;
  background: var(--bg2);
  position: relative;
  overflow: hidden;
}
.process-section::before {
  content: '';
  position: absolute;
  inset: 0;
  background:
    radial-gradient(ellipse 50% 60% at 0% 50%, rgba(167,139,250,0.06) 0%, transparent 60%),
    radial-gradient(ellipse 40% 50% at 100% 50%, rgba(34,211,238,0.05) 0%, transparent 60%);
  pointer-events: none;
}
.process-inner {
  max-width: 1380px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
}
.process-header {
  text-align: center;
  margin-bottom: 72px;
}
.process-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 2px;
  position: relative;
}
/* connecting line */
.process-grid::before {
  content: '';
  position: absolute;
  top: 40px;
  left: calc(12.5% + 24px);
  right: calc(12.5% + 24px);
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--border-bright) 20%, var(--border-bright) 80%, transparent);
  z-index: 0;
}

.process-step {
  padding: 0 24px;
  position: relative;
  z-index: 1;
  text-align: center;
}
.process-num-wrap {
  display: flex;
  justify-content: center;
  margin-bottom: 24px;
}
.process-num {
  width: 56px; height: 56px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-display);
  font-size: 18px;
  font-weight: 800;
  position: relative;
  background: var(--bg2);
  border: 0.5px solid var(--border-bright);
  color: var(--text);
  transition: all 0.3s var(--ease);
}
.process-step:hover .process-num {
  background: linear-gradient(135deg, var(--accent-purple), var(--accent-cyan));
  color: #06060a;
  border-color: transparent;
  box-shadow: 0 0 30px rgba(167,139,250,0.4);
}

.process-step-title {
  font-family: var(--font-display);
  font-size: 18px;
  font-weight: 700;
  color: var(--text);
  margin-bottom: 12px;
  letter-spacing: -0.02em;
}
.process-step-desc {
  font-size: 14px;
  line-height: 1.75;
  color: var(--text-muted);
}

/* ============================================================
   TECH STACK
============================================================ */
.tech-section {
  padding: 80px 48px;
}
.tech-inner {
  max-width: 1380px;
  margin: 0 auto;
}
.tech-header {
  text-align: center;
  margin-bottom: 48px;
}
.tech-grid {
  display: grid;
  grid-template-columns: repeat(6, 1fr);
  gap: 12px;
}
.tech-pill {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
  padding: 22px 16px;
  border-radius: var(--radius);
  background: var(--surface);
  border: 0.5px solid var(--border);
  transition: all 0.3s var(--ease);
  cursor: default;
}
.tech-pill:hover {
  background: var(--surface-hover);
  border-color: var(--border-bright);
  transform: translateY(-3px);
}
.tech-icon { font-size: 26px; }
.tech-name {
  font-size: 12px;
  font-weight: 600;
  color: var(--text-muted);
  letter-spacing: 0.04em;
  text-align: center;
}

/* ============================================================
   TESTIMONIALS
============================================================ */
.testimonials-section {
  padding: 80px 48px;
  background: var(--bg2);
}
.testimonials-inner { max-width: 1380px; margin: 0 auto; }
.testimonials-header { text-align: center; margin-bottom: 56px; }

.testimonials-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}
.testimonial-card {
  padding: 32px;
  border-radius: var(--radius-lg);
  background: rgba(255,255,255,0.03);
  border: 0.5px solid var(--border);
  position: relative;
  overflow: hidden;
  transition: border-color 0.3s, transform 0.3s var(--ease);
}
.testimonial-card::before {
  content: '\201C';
  position: absolute;
  top: 16px; left: 24px;
  font-family: var(--font-display);
  font-size: 72px;
  line-height: 1;
  color: var(--accent-purple);
  opacity: 0.15;
  font-weight: 800;
}
.testimonial-card:hover {
  border-color: var(--border-bright);
  transform: translateY(-4px);
}
.testimonial-text {
  font-size: 15px;
  line-height: 1.8;
  color: var(--text-muted);
  margin-bottom: 28px;
  position: relative;
  z-index: 1;
  font-style: italic;
  font-weight: 300;
}
.testimonial-author {
  display: flex;
  align-items: center;
  gap: 12px;
}
.testimonial-avatar {
  width: 40px; height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-display);
  font-size: 14px;
  font-weight: 700;
  color: #06060a;
}
.av-purple { background: linear-gradient(135deg, #a78bfa, #c4b5fd); }
.av-cyan   { background: linear-gradient(135deg, #22d3ee, #67e8f9); }
.av-pink   { background: linear-gradient(135deg, #f472b6, #f9a8d4); }

.testimonial-name {
  font-size: 14px;
  font-weight: 600;
  color: var(--text);
}
.testimonial-role {
  font-size: 12px;
  color: var(--text-dim);
  margin-top: 2px;
}
.stars {
  color: var(--accent-amber);
  font-size: 13px;
  letter-spacing: 2px;
  margin-bottom: 16px;
}

/* ============================================================
   CONTACT
============================================================ */
.contact-section {
  padding: 120px 48px;
  position: relative;
  overflow: hidden;
}
.contact-section::before {
  content: '';
  position: absolute;
  inset: 0;
  background:
    radial-gradient(ellipse 50% 50% at 20% 50%, rgba(167,139,250,0.08) 0%, transparent 60%),
    radial-gradient(ellipse 40% 50% at 80% 50%, rgba(34,211,238,0.06) 0%, transparent 60%);
}
.contact-inner {
  max-width: 1380px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: 0.9fr 1.1fr;
  gap: 80px;
  align-items: start;
}

.contact-copy .section-heading { margin-bottom: 20px; }
.contact-sub {
  font-size: 16px;
  line-height: 1.8;
  color: var(--text-muted);
  font-weight: 300;
  margin-bottom: 40px;
}

.contact-points {
  display: flex;
  flex-direction: column;
  gap: 16px;
}
.contact-point {
  padding: 20px;
  border-radius: var(--radius);
  background: var(--surface);
  border: 0.5px solid var(--border);
  transition: border-color 0.3s, background 0.3s;
}
.contact-point:hover { border-color: var(--border-bright); background: var(--surface-hover); }
.contact-point-title {
  font-size: 14px;
  font-weight: 600;
  color: var(--text);
  margin-bottom: 6px;
  display: flex;
  align-items: center;
  gap: 8px;
}
.contact-point-title span {
  font-size: 16px;
}
.contact-point-body {
  font-size: 13px;
  color: var(--text-muted);
  line-height: 1.6;
}

.form-wrap {
  padding: 36px;
  border-radius: var(--radius-lg);
  background: rgba(255,255,255,0.04);
  backdrop-filter: blur(20px);
  border: 0.5px solid var(--border);
  box-shadow: 0 40px 80px rgba(0,0,0,0.3);
}
.form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-bottom: 16px;
}
.field {
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.field-label {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--text-dim);
}
.field input,
.field select,
.field textarea {
  padding: 14px 16px;
  border-radius: 14px;
  background: rgba(255,255,255,0.04);
  border: 0.5px solid var(--border);
  color: var(--text);
  font-family: var(--font-body);
  font-size: 14px;
  outline: none;
  transition: border-color 0.25s, background 0.25s;
  width: 100%;
  -webkit-appearance: none;
}
.field input::placeholder,
.field textarea::placeholder { color: var(--text-dim); }
.field input:focus,
.field select:focus,
.field textarea:focus {
  border-color: rgba(167,139,250,0.5);
  background: rgba(167,139,250,0.04);
}
.field select option { background: #12121c; color: var(--text); }
.field-full { grid-column: 1 / -1; }
.field textarea { resize: none; }

.submit-btn {
  width: 100%;
  padding: 16px;
  border-radius: 999px;
  background: linear-gradient(135deg, #a78bfa, #22d3ee);
  border: none;
  color: #06060a;
  font-family: var(--font-display);
  font-size: 15px;
  font-weight: 700;
  letter-spacing: 0.02em;
  cursor: pointer;
  margin-top: 8px;
  transition: transform 0.3s var(--ease), box-shadow 0.3s, opacity 0.3s;
  box-shadow: 0 0 30px rgba(167,139,250,0.25);
}
.submit-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 40px rgba(167,139,250,0.4);
}

/* ============================================================
   FOOTER
============================================================ */
footer {
  padding: 60px 48px 40px;
  border-top: 0.5px solid var(--border);
  background: var(--bg);
}
.footer-inner {
  max-width: 1380px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr;
  gap: 48px;
  margin-bottom: 48px;
}
.footer-brand-logo {
  font-family: var(--font-display);
  font-size: 22px;
  font-weight: 800;
  letter-spacing: -0.03em;
  background: var(--grad-1);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 16px;
}
.footer-brand-desc {
  font-size: 14px;
  line-height: 1.75;
  color: var(--text-dim);
  max-width: 280px;
  margin-bottom: 24px;
}
.footer-socials {
  display: flex;
  gap: 10px;
}
.footer-social {
  width: 36px; height: 36px;
  border-radius: 50%;
  background: var(--surface);
  border: 0.5px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-dim);
  text-decoration: none;
  font-size: 14px;
  transition: all 0.3s;
}
.footer-social:hover {
  background: var(--surface-hover);
  border-color: var(--border-bright);
  color: var(--text);
}
.footer-col-title {
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--text-muted);
  margin-bottom: 20px;
}
.footer-links {
  display: flex;
  flex-direction: column;
  gap: 12px;
  list-style: none;
}
.footer-links a {
  font-size: 14px;
  color: var(--text-dim);
  text-decoration: none;
  transition: color 0.25s;
}
.footer-links a:hover { color: var(--text-muted); }
.footer-bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 32px;
  border-top: 0.5px solid var(--border);
}
.footer-copy {
  font-size: 13px;
  color: var(--text-dim);
}
.footer-made {
  font-size: 13px;
  color: var(--text-dim);
  display: flex;
  align-items: center;
  gap: 6px;
}
.footer-made span {
  background: var(--grad-1);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  font-weight: 600;
}

/* ============================================================
   RESPONSIVE
============================================================ */
@media (max-width: 1100px) {
  .hero-inner { grid-template-columns: 1fr; }
  .hero-panel { max-width: 680px; }
  .services-grid { grid-template-columns: repeat(2, 1fr); }
  .about-grid { grid-template-columns: 1fr; gap: 48px; }
  .about-visual { max-width: 480px; }
  .process-grid { grid-template-columns: repeat(2, 1fr); gap: 32px; }
  .process-grid::before { display: none; }
  .tech-grid { grid-template-columns: repeat(3, 1fr); }
  .testimonials-grid { grid-template-columns: repeat(2, 1fr); }
  .contact-inner { grid-template-columns: 1fr; gap: 48px; }
  .footer-inner { grid-template-columns: 1fr 1fr; }
}

@media (max-width: 768px) {
  nav { padding: 16px 24px; }
  .nav-links { display: none; }
  .hero { padding: 100px 24px 60px; }
  .hero h1 { font-size: 3rem; }
  .hero-stats { gap: 24px; }
  .section { padding: 80px 24px; }
  .section-full { padding: 80px 24px; }
  .services-grid { grid-template-columns: 1fr; }
  .process-grid { grid-template-columns: 1fr; }
  .tech-grid { grid-template-columns: repeat(2, 1fr); }
  .testimonials-grid { grid-template-columns: 1fr; }
  .form-grid { grid-template-columns: 1fr; }
  .footer-inner { grid-template-columns: 1fr; gap: 32px; }
  .footer-bottom { flex-direction: column; gap: 12px; text-align: center; }
  .hero-panel-row { grid-template-columns: 1fr; }
  .contact-section { padding: 80px 24px; }
  .portfolio-placeholder { margin: 0 24px 60px; }
  .about-stats { grid-template-columns: 1fr; }
  .services-header { flex-direction: column; align-items: flex-start; }
}
</style>
</head>
<body>

<!-- CURSOR -->
<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursor-ring"></div>

<!-- ============================================================
     NAV
============================================================ -->
<nav id="nav">
  <a href="#" class="nav-logo">Ardo Productions.</a>
  <ul class="nav-links">
    <li><a href="#services">Services</a></li>
    <li><a href="#portfolio">Portfolio</a></li>
    <li><a href="#about">About</a></li>
    <li><a href="#process">Process</a></li>
    <li><a href="#contact">Contact</a></li>
    <li>
      <div class="avail-badge">
        <div class="avail-dot"></div>
        Available for projects
      </div>
    </li>
    <li><a href="#contact" class="nav-cta">Start a Project</a></li>
  </ul>
</nav>

<!-- ============================================================
     HERO
============================================================ -->
<section class="hero">
  <div class="orb orb-1"></div>
  <div class="orb orb-2"></div>
  <div class="orb orb-3"></div>
  <div class="hero-grid"></div>

  <div class="hero-inner">
    <div class="hero-copy">
      <div class="hero-eyebrow">
        <span class="hero-eyebrow-line"></span>
        Johannesburg, South Africa
      </div>

      <h1>
        Design.<br>
        <span class="grad-text">Build.</span><br>
        Activate.
      </h1>

      <p class="hero-sub">
        Ardo Productions creates high-impact brand experiences, stunning 3D visuals,
        cinematic environments — and builds custom Wix apps and widgets that make your
        digital presence impossible to ignore.
      </p>

      <div class="hero-ctas">
        <a href="#portfolio" class="btn-primary">
          <span>View Portfolio</span>
          <span>→</span>
        </a>
        <a href="#contact" class="btn-secondary">Start a Project</a>
      </div>

      <div class="hero-stats">
        <div class="hero-stat">
          <div class="hero-stat-num">45+</div>
          <div class="hero-stat-label">Projects</div>
        </div>
        <div class="hero-stat">
          <div class="hero-stat-num">25+</div>
          <div class="hero-stat-label">Clients</div>
        </div>
        <div class="hero-stat">
          <div class="hero-stat-num">9+</div>
          <div class="hero-stat-label">Years exp.</div>
        </div>
      </div>
    </div>

    <div class="hero-panel">
      <div class="glass-card glass-card-main">
        <div class="card-tag">Studio Positioning</div>
        <h3>Built for brands that demand presence, not noise.</h3>
        <p>Spatial storytelling meets digital precision. From expo floors to Wix Marketplace — one studio, every dimension.</p>
      </div>
      <div class="hero-panel-row">
        <div class="glass-card">
          <div class="mini-label">Core Disciplines</div>
          <div class="mini-items">
            <div class="mini-item">Spatial & Brand Design</div>
            <div class="mini-item">3D Visualisation</div>
            <div class="mini-item">Wix App Development</div>
          </div>
        </div>
        <div class="glass-card">
          <div class="mini-label">Based in</div>
          <div class="mini-items">
            <div class="mini-item">Johannesburg</div>
            <div class="mini-item">South Africa</div>
            <div class="mini-item">Remote-friendly</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ============================================================
     TRUST STRIP
============================================================ -->
<div class="trust-section">
  <p class="trust-label">Trusted by leading brands</p>
  <div class="trust-track-wrap">
    <div class="trust-track" id="trust-track">
      <div class="logo-pill">Shell</div>
      <div class="logo-pill">Dopamine Coffee</div>
      <div class="logo-pill">O-London</div>
      <div class="logo-pill">Brand Partner</div>
      <div class="logo-pill">Creative Studio</div>
      <div class="logo-pill">Agency Client</div>
      <div class="logo-pill">Enterprise Co.</div>
      <div class="logo-pill">Shell</div>
      <div class="logo-pill">Dopamine Coffee</div>
      <div class="logo-pill">O-London</div>
      <div class="logo-pill">Brand Partner</div>
      <div class="logo-pill">Creative Studio</div>
      <div class="logo-pill">Agency Client</div>
      <div class="logo-pill">Enterprise Co.</div>
    </div>
  </div>
</div>

<!-- ============================================================
     SERVICES
============================================================ -->
<section class="section" id="services">
  <div class="services-header reveal">
    <div>
      <div class="section-eyebrow">What I do</div>
      <h2 class="section-heading">Every dimension<br>of your brand.</h2>
    </div>
    <p class="section-sub">From immersive physical spaces to custom-coded digital experiences — I handle it all under one roof.</p>
  </div>

  <div class="services-grid">
    <!-- Brand Activation -->
    <div class="service-tile tile-purple reveal reveal-delay-1">
      <div class="service-icon icon-purple">🎯</div>
      <div class="service-name">Brand Activation</div>
      <div class="service-desc">Immersive on-the-ground experiences that captivate audiences and make your brand impossible to forget.</div>
      <div class="service-tags">
        <span class="service-tag">Festival Activations</span>
        <span class="service-tag">Pop-up Experiences</span>
        <span class="service-tag">Interactive Installations</span>
      </div>
    </div>

    <!-- Expo Stands -->
    <div class="service-tile tile-cyan reveal reveal-delay-2">
      <div class="service-icon icon-cyan">🏛️</div>
      <div class="service-name">Expo Stands</div>
      <div class="service-desc">Custom-designed stands that stop people in their tracks. Every detail designed to draw visitors in and hold their attention.</div>
      <div class="service-tags">
        <span class="service-tag">Custom Stand Design</span>
        <span class="service-tag">3D Visualisation</span>
        <span class="service-tag">Full Build & Install</span>
      </div>
    </div>

    <!-- 3D Rendering -->
    <div class="service-tile tile-pink reveal reveal-delay-3">
      <div class="service-icon icon-pink">🎨</div>
      <div class="service-name">3D Rendering</div>
      <div class="service-desc">Hyper-realistic visualisations that let you see your project before a single brick is laid. Concepts made tangible.</div>
      <div class="service-tags">
        <span class="service-tag">Realistic Renders</span>
        <span class="service-tag">Material & Lighting</span>
        <span class="service-tag">Virtual Walkthroughs</span>
      </div>
    </div>

    <!-- Set Design -->
    <div class="service-tile tile-amber reveal reveal-delay-1">
      <div class="service-icon icon-amber">🎬</div>
      <div class="service-name">Set Design</div>
      <div class="service-desc">Unforgettable environments for TV, film, and live events. Where artistic vision meets practical execution.</div>
      <div class="service-tags">
        <span class="service-tag">TV & Film Sets</span>
        <span class="service-tag">Talk Show Environments</span>
        <span class="service-tag">Music Video Sets</span>
      </div>
    </div>

    <!-- Website Design -->
    <div class="service-tile tile-teal reveal reveal-delay-2">
      <div class="service-icon icon-teal">💻</div>
      <div class="service-name">Website Design</div>
      <div class="service-desc">Visually stunning, strategically sharp websites tailored to your brand. Built to convert and built to impress.</div>
      <div class="service-tags">
        <span class="service-tag">Custom Web Design</span>
        <span class="service-tag">UI/UX Optimisation</span>
        <span class="service-tag">Responsive Layouts</span>
      </div>
    </div>

    <!-- Graphic Design -->
    <div class="service-tile tile-purple reveal reveal-delay-3">
      <div class="service-icon icon-purple">✏️</div>
      <div class="service-name">Graphic Design</div>
      <div class="service-desc">Brand identity, print, and digital design that tells your story with visual clarity and creative precision.</div>
      <div class="service-tags">
        <span class="service-tag">Brand Identity</span>
        <span class="service-tag">Print Design</span>
        <span class="service-tag">Digital Assets</span>
      </div>
    </div>

    <!-- Wix App Dev -->
    <div class="service-tile tile-wix reveal reveal-delay-1">
      <div class="wix-badge">New</div>
      <div class="service-icon icon-purple">⚡</div>
      <div class="service-name">Wix App Development</div>
      <div class="service-desc">Custom Wix applications built with Velo that extend what your site can do — from booking flows to complex integrations.</div>
      <div class="service-tags">
        <span class="service-tag">Velo / JavaScript</span>
        <span class="service-tag">Custom Integrations</span>
        <span class="service-tag">Wix Marketplace</span>
      </div>
    </div>

    <!-- Wix Widget Dev -->
    <div class="service-tile tile-wix reveal reveal-delay-2">
      <div class="wix-badge">New</div>
      <div class="service-icon icon-cyan">🧩</div>
      <div class="service-name">Wix Widget Development</div>
      <div class="service-desc">Bespoke interactive widgets embedded directly into Wix — this page is a live example of what's possible.</div>
      <div class="service-tags">
        <span class="service-tag">Embedded HTML</span>
        <span class="service-tag">Custom Components</span>
        <span class="service-tag">Interactive UI</span>
      </div>
    </div>
  </div>
</section>

<!-- ============================================================
     PORTFOLIO PLACEHOLDER
============================================================ -->
<div class="portfolio-placeholder reveal" id="portfolio">
  <div class="portfolio-placeholder-inner">
    <div class="portfolio-placeholder-icon">🖼️</div>
    <h3>Featured Work — Wix Pro Gallery</h3>
    <p>Replace this section with your Wix Pro Gallery block.<br>Filter by Brand Activation · Expo Stand · 3D Rendering · Set Design · Web & Wix Dev</p>
  </div>
</div>

<!-- ============================================================
     ABOUT
============================================================ -->
<section class="section" id="about">
  <div class="about-grid">
    <div class="about-visual reveal">
      <div class="about-image-frame">
        <div class="about-image-placeholder">RM</div>
        <div class="about-name-card">
          <div class="about-name">Riccardo Montali</div>
          <div class="about-title-tag">Founder · Designer · Developer</div>
        </div>
      </div>
      <div class="floating-badge">
        <div class="floating-badge-num">9+</div>
        <div class="floating-badge-text">Years of craft</div>
      </div>
    </div>

    <div class="about-copy reveal reveal-delay-2">
      <div class="section-eyebrow">About</div>
      <h2 class="section-heading">Not just a designer.<br>A storyteller.</h2>
      <p class="about-body">
        I'm <strong>Riccardo Montali</strong> — the one-person force behind Ardo Productions. I believe every brand has a unique story to tell, and my mission is to bring that story to life in ways that are bold, engaging, and impossible to ignore.
      </p>
      <p class="about-body">
        From building <strong>immersive expo stands</strong> for global brands to writing <strong>custom Wix apps and widgets</strong> from scratch, I combine artistry with technical precision to deliver work that genuinely moves the needle.
      </p>
      <p class="about-body">
        When a client works with Ardo Productions, they get direct access to the person doing the work — no account managers, no handoffs, no diluted vision.
      </p>

      <div class="about-stats">
        <div class="about-stat">
          <div class="about-stat-num">45+</div>
          <div class="about-stat-label">Projects Done</div>
        </div>
        <div class="about-stat">
          <div class="about-stat-num">25+</div>
          <div class="about-stat-label">Happy Clients</div>
        </div>
        <div class="about-stat">
          <div class="about-stat-num">9+</div>
          <div class="about-stat-label">Years Exp.</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ============================================================
     PROCESS
============================================================ -->
<div class="process-section" id="process">
  <div class="process-inner">
    <div class="process-header reveal">
      <div class="section-eyebrow">How it works</div>
      <h2 class="section-heading">From brief to brilliant.</h2>
    </div>
    <div class="process-grid">
      <div class="process-step reveal reveal-delay-1">
        <div class="process-num-wrap">
          <div class="process-num">01</div>
        </div>
        <div class="process-step-title">Discovery</div>
        <div class="process-step-desc">I dive deep into your brand, goals, and audience to understand your unique story and the problem we're actually solving.</div>
      </div>
      <div class="process-step reveal reveal-delay-2">
        <div class="process-num-wrap">
          <div class="process-num">02</div>
        </div>
        <div class="process-step-title">Design</div>
        <div class="process-step-desc">Stunning 3D renders, wireframes, and concepts that bring your vision to life before a single brick is laid or a line of code is written.</div>
      </div>
      <div class="process-step reveal reveal-delay-3">
        <div class="process-num-wrap">
          <div class="process-num">03</div>
        </div>
        <div class="process-step-title">Execute</div>
        <div class="process-step-desc">Every detail of production, build, or development handled with precision — whether it's a physical stand or a custom Wix widget.</div>
      </div>
      <div class="process-step reveal reveal-delay-4">
        <div class="process-num-wrap">
          <div class="process-num">04</div>
        </div>
        <div class="process-step-title">Deliver</div>
        <div class="process-step-desc">Your brand experience launches on time, on budget, and exceeds expectations. Then I'm still here for whatever comes next.</div>
      </div>
    </div>
  </div>
</div>

<!-- ============================================================
     TECH STACK
============================================================ -->
<div class="tech-section">
  <div class="tech-inner">
    <div class="tech-header reveal">
      <div class="section-eyebrow">Tools & Tech</div>
      <h2 class="section-heading">The toolkit.</h2>
    </div>
    <div class="tech-grid">
      <div class="tech-pill reveal reveal-delay-1">
        <div class="tech-icon">🌐</div>
        <div class="tech-name">Wix Studio</div>
      </div>
      <div class="tech-pill reveal reveal-delay-2">
        <div class="tech-icon">⚡</div>
        <div class="tech-name">Velo by Wix</div>
      </div>
      <div class="tech-pill reveal reveal-delay-3">
        <div class="tech-icon">🟨</div>
        <div class="tech-name">JavaScript</div>
      </div>
      <div class="tech-pill reveal reveal-delay-4">
        <div class="tech-icon">📐</div>
        <div class="tech-name">3ds Max</div>
      </div>
      <div class="tech-pill reveal reveal-delay-1">
        <div class="tech-icon">🎨</div>
        <div class="tech-name">Adobe CC</div>
      </div>
      <div class="tech-pill reveal reveal-delay-2">
        <div class="tech-icon">🧊</div>
        <div class="tech-name">V-Ray</div>
      </div>
      <div class="tech-pill reveal reveal-delay-3">
        <div class="tech-icon">🖌️</div>
        <div class="tech-name">Figma</div>
      </div>
      <div class="tech-pill reveal reveal-delay-4">
        <div class="tech-icon">🏗️</div>
        <div class="tech-name">AutoCAD</div>
      </div>
      <div class="tech-pill reveal reveal-delay-1">
        <div class="tech-icon">📦</div>
        <div class="tech-name">SketchUp</div>
      </div>
      <div class="tech-pill reveal reveal-delay-2">
        <div class="tech-icon">🌟</div>
        <div class="tech-name">After Effects</div>
      </div>
      <div class="tech-pill reveal reveal-delay-3">
        <div class="tech-icon">💬</div>
        <div class="tech-name">HTML / CSS</div>
      </div>
      <div class="tech-pill reveal reveal-delay-4">
        <div class="tech-icon">🔗</div>
        <div class="tech-name">REST APIs</div>
      </div>
    </div>
  </div>
</div>

<!-- ============================================================
     TESTIMONIALS
============================================================ -->
<div class="testimonials-section">
  <div class="testimonials-inner">
    <div class="testimonials-header reveal">
      <div class="section-eyebrow">Client feedback</div>
      <h2 class="section-heading">What people say.</h2>
    </div>
    <div class="testimonials-grid">
      <div class="testimonial-card reveal reveal-delay-1">
        <div class="stars">★★★★★</div>
        <p class="testimonial-text">"Riccardo completely transformed how we show up at expos. The stand he designed stopped people in their tracks — we had more leads from that one event than the previous three combined."</p>
        <div class="testimonial-author">
          <div class="testimonial-avatar av-purple">JM</div>
          <div>
            <div class="testimonial-name">Jamie M.</div>
            <div class="testimonial-role">Marketing Director</div>
          </div>
        </div>
      </div>
      <div class="testimonial-card reveal reveal-delay-2">
        <div class="stars">★★★★★</div>
        <p class="testimonial-text">"The custom Wix widget he built for us does things I didn't even know were possible on Wix. Our clients are genuinely amazed when they see what the site can do — and so are we."</p>
        <div class="testimonial-author">
          <div class="testimonial-avatar av-cyan">SK</div>
          <div>
            <div class="testimonial-name">Sarah K.</div>
            <div class="testimonial-role">E-commerce Founder</div>
          </div>
        </div>
      </div>
      <div class="testimonial-card reveal reveal-delay-3">
        <div class="stars">★★★★★</div>
        <p class="testimonial-text">"Working with Ardo Productions felt like having a full agency at our disposal. The 3D renders were stunning, the set came together perfectly, and every deadline was hit. Will not go anywhere else."</p>
        <div class="testimonial-author">
          <div class="testimonial-avatar av-pink">TN</div>
          <div>
            <div class="testimonial-name">Thabo N.</div>
            <div class="testimonial-role">TV Production Lead</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- ============================================================
     CONTACT
============================================================ -->
<section class="contact-section" id="contact">
  <div class="contact-inner">
    <div class="contact-copy">
      <div class="section-eyebrow">Let's work together</div>
      <h2 class="section-heading">Bring the brief.<br><span style="background:var(--grad-3);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;">I'll shape the impact.</span></h2>
      <p class="contact-sub">Every great project starts with a conversation. Tell me what you're building — whether it's a brand activation, a custom Wix app, or something in between — and I'll guide the next step.</p>

      <div class="contact-points">
        <div class="contact-point">
          <div class="contact-point-title"><span>📍</span> Based in Johannesburg</div>
          <div class="contact-point-body">South Africa · Available for remote and travel projects worldwide.</div>
        </div>
        <div class="contact-point">
          <div class="contact-point-title"><span>✉️</span> riccardo@ardoproductions.com</div>
          <div class="contact-point-body">I respond to every enquiry personally, usually within 24 hours.</div>
        </div>
        <div class="contact-point">
          <div class="contact-point-title"><span>📲</span> Instagram & LinkedIn</div>
          <div class="contact-point-body">@ardo.productions · Follow along for process, work in progress, and behind the scenes.</div>
        </div>
      </div>
    </div>

    <div class="form-wrap reveal">
      <form>
        <div class="form-grid">
          <div class="field">
            <label class="field-label">Full Name</label>
            <input type="text" placeholder="Your name" />
          </div>
          <div class="field">
            <label class="field-label">Email</label>
            <input type="email" placeholder="you@company.com" />
          </div>
          <div class="field">
            <label class="field-label">Company / Brand</label>
            <input type="text" placeholder="Company name" />
          </div>
          <div class="field">
            <label class="field-label">Phone</label>
            <input type="text" placeholder="+27 ..." />
          </div>
        </div>
        <div class="field" style="margin-bottom:16px;">
          <label class="field-label">What do you need?</label>
          <select>
            <option>Select a service</option>
            <option>Brand Activation</option>
            <option>Expo Stand Design</option>
            <option>3D Rendering</option>
            <option>Set Design</option>
            <option>Website Design</option>
            <option>Graphic Design / Branding</option>
            <option>Wix App Development</option>
            <option>Wix Widget Development</option>
            <option>Not sure yet — let's talk</option>
          </select>
        </div>
        <div class="form-grid">
          <div class="field">
            <label class="field-label">Budget Range</label>
            <select>
              <option>Select a range</option>
              <option>Under R15,000</option>
              <option>R15,000 – R40,000</option>
              <option>R40,000 – R80,000</option>
              <option>R80,000+</option>
              <option>Prefer to discuss</option>
            </select>
          </div>
          <div class="field">
            <label class="field-label">Timeline</label>
            <select>
              <option>Select timing</option>
              <option>Urgent (ASAP)</option>
              <option>Within 2 weeks</option>
              <option>Within 1 month</option>
              <option>Flexible</option>
            </select>
          </div>
        </div>
        <div class="field" style="margin-top:16px;margin-bottom:20px;">
          <label class="field-label">Tell me about the project</label>
          <textarea rows="5" placeholder="Share the vision, goals, and anything important I should know."></textarea>
        </div>
        <button type="submit" class="submit-btn">Submit Project Enquiry →</button>
      </form>
    </div>
  </div>
</section>

<!-- ============================================================
     FOOTER
============================================================ -->
<footer>
  <div class="footer-inner">
    <div>
      <div class="footer-brand-logo">Ardo Productions.</div>
      <p class="footer-brand-desc">Transforming brands into unforgettable experiences through bold design, precise execution, and custom digital development.</p>
      <div class="footer-socials">
        <a href="https://instagram.com/ardo.productions" class="footer-social" target="_blank" rel="noopener">ig</a>
        <a href="https://linkedin.com/in/riccardo-montali" class="footer-social" target="_blank" rel="noopener">in</a>
        <a href="mailto:riccardo@ardoproductions.com" class="footer-social">@</a>
      </div>
    </div>
    <div>
      <div class="footer-col-title">Services</div>
      <ul class="footer-links">
        <li><a href="#services">Brand Activation</a></li>
        <li><a href="#services">Expo Stands</a></li>
        <li><a href="#services">3D Rendering</a></li>
        <li><a href="#services">Set Design</a></li>
        <li><a href="#services">Website Design</a></li>
        <li><a href="#services">Wix App Dev</a></li>
        <li><a href="#services">Wix Widgets</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-col-title">Studio</div>
      <ul class="footer-links">
        <li><a href="#about">About Riccardo</a></li>
        <li><a href="#portfolio">Portfolio</a></li>
        <li><a href="#process">Process</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-col-title">Contact</div>
      <ul class="footer-links">
        <li><a href="mailto:riccardo@ardoproductions.com">riccardo@ardoproductions.com</a></li>
        <li><a href="#">Johannesburg, South Africa</a></li>
        <li><a href="https://instagram.com/ardo.productions" target="_blank" rel="noopener">Instagram</a></li>
        <li><a href="https://linkedin.com/in/riccardo-montali" target="_blank" rel="noopener">LinkedIn</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <p class="footer-copy">© 2025 Ardo Productions. All rights reserved.</p>
    <p class="footer-made">Designed & built by <span>Riccardo Montali</span></p>
  </div>
</footer>

<!-- ============================================================
     JS
============================================================ -->
<script>
// Custom cursor
const cursor = document.getElementById('cursor');
const ring = document.getElementById('cursor-ring');
let mouseX = 0, mouseY = 0, ringX = 0, ringY = 0;

document.addEventListener('mousemove', e => {
  mouseX = e.clientX;
  mouseY = e.clientY;
  cursor.style.left = mouseX + 'px';
  cursor.style.top = mouseY + 'px';
});

function animateRing() {
  ringX += (mouseX - ringX) * 0.12;
  ringY += (mouseY - ringY) * 0.12;
  ring.style.left = ringX + 'px';
  ring.style.top = ringY + 'px';
  requestAnimationFrame(animateRing);
}
animateRing();

// Nav scroll
const nav = document.getElementById('nav');
window.addEventListener('scroll', () => {
  nav.classList.toggle('scrolled', window.scrollY > 40);
});

// Scroll reveal
const reveals = document.querySelectorAll('.reveal');
const observer = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) {
      e.target.classList.add('visible');
    }
  });
}, { threshold: 0.12 });

reveals.forEach(el => observer.observe(el));
</script>
</body>
</html>
